{
  "name": "finlly",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo",
  "scripts": {
    "dev": "cd apps/api && npm run dev & cd ../web && npm run dev",
    "build": "cd apps/api && npm run build && cd ../web && npm run build",
    "test": "echo 'Running tests...'"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.4"
  }
}
