{
  "name": "finlly",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "dev": "concurrently \"npm --workspace=apps/api run dev\" \"npm --workspace=apps/web run dev\"",
    "build": "npm --workspace=apps/api run build && npm --workspace=apps/web run build",
    "lint": "npm --workspace=apps/api run lint && npm --workspace=apps/web run lint",
    "lint:fix": "npm --workspace=apps/api run lint:fix && npm --workspace=apps/web run lint:fix",
    "format": "npm --workspace=apps/api run format && npm --workspace=apps/web run format",
    "format:check": "npm --workspace=apps/api run format:check && npm --workspace=apps/web run format:check",
    "test": "npm --workspace=apps/api run test && npm --workspace=apps/web run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.4",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  },
  "dependencies": {
    "eslint": "^9.11.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.37.0",
    "eslint-plugin-react-hooks": "^5.0.0",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.3"
  }
}
